'use client';

import styles from './savings-page.module.scss';

export function SavingsPage() {
  return (
    <main className={styles.pageWrapper}>
      <div className={styles.contentWrapper}>
        <h1>Savings</h1>
      </div>
    </main>
  );
}
